@model IEnumerable<LiveCensus.Models.shift>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.shiftNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiftDescr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.shiftNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiftDescr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShiftId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShiftId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShiftId })
        </td>
    </tr>
}

</table>
